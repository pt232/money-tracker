{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=./apps/*",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
